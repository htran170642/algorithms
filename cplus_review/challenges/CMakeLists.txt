# Coding challenges, in C++, per the CLAUDE.md "LeetCode Rules":
#
#   Problem -> Questions -> Hints -> Pseudocode -> YOUR implementation
#           -> Review -> Optimal -> Alternative -> Interview follow-up
#
# ~2/week. These are STL and idiom reps, not algorithm reps — the point is
# fluency with the containers and algorithms you'll actually reach for under
# interview pressure, not rediscovering that two pointers exist.

cr_add_test(challenges_test SOURCES
  tests/placeholder_test.cpp
)
